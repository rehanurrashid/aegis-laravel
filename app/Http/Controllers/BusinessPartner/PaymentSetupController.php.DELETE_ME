DELETE THIS FILE:
  app/Http/Controllers/BusinessPartner/PaymentSetupController.php

REASON:
  Replaced by CS-style Stripe Connect Express onboarding on
  BP/SettingsController::connectOnboard() + connectReturn().
  The old controller stored a manually-typed account_id and never
  invoked Stripe — that flow cannot work in production.

ALSO DELETE:
  resources/js/pages/business-partner/PaymentSetup.vue
    (fake bank-account/ACH/PayPal form; posted to non-existent route)

Both the controller import (BpPaymentController alias) and the two
`/payment-setup` routes must be removed from routes/web.php — see the
patched routes/web.php in this ZIP.
